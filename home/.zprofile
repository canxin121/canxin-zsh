# This file is sourced by the managed block installed in ~/.zprofile.
# Machine-specific profile settings remain in ~/.zprofile.local.

ZSH_DOTFILES_HOME="${${(%):-%N}:A:h}"
ZSH_DOTFILES_ROOT="${ZSH_DOTFILES_HOME:h}"
